# 测试夹具复用设计

## 目标
减少临时工作区创建/销毁，同时不让不同测试用例共享残留状态。

## 具体方案
在 tests/helpers/cli.ts 中维护固定槽位池：首次使用槽位创建临时根目录，后续用例复用该根；每次获取前递归清空 `.workflow`，cleanup 只清空并释放槽位，同一用例中并发获取仍分配不同槽位。进程退出时同步清理所有池目录。

## 验收方式
新增回归用例验证槽位复用、获取前隔离、同一用例多工作区隔离和退出清理注册；现有全量测试、影响测试和 verify-demo 通过，并记录测试耗时。
