# 权威说明

## 目标
用固定测试夹具框架复用临时工作区，减少每个用例重复创建和销毁工作区的开销，同时保持测试隔离和 CLI 集成覆盖。

## 范围
- tests/helpers/cli.ts 的工作区槽位池和清理生命周期。
- 夹具复用与隔离的回归测试和耗时对比。

## 非目标
- 不改变测试断言、CLI 行为或生产代码。
- 不在本轮引入并行执行或常驻 CLI 进程。

## 已确认要求
- 用户于 2026-08-29 确认测试文件级固定夹具、用例级基线恢复方案。

## 必须遵守的约束

## 审批委托
- 状态：常规
- 范围：无
- 授权依据：无
