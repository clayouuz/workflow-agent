# T8 复杂任务讨论状态机设计

## 目标
复杂任务明确记录目标、具体方案和验收方式；简单任务不创建状态机，非修改类任务可复用同一流程。

## 具体方案
新增 `discussion-state.json` 和 `ledger discussion` 命令，状态与确认哈希由 CLI 管理，语义设计文档由 LLM 编辑。扩展模板到 v9，保留 v8 迁移兼容；确认链完整后授予 `execution_permitted`。

## 验收方式
自动测试覆盖完整确认链、非法跳转、设计变更失效、简单任务旁路、非修改类任务和迁移兼容；全量测试与 verify-demo 必须通过。
