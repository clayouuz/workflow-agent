# 权威说明

## 目标
移除面向用户的 workflow-agent 绝对路径依赖，支持 `~/workflow-agent` 等可移植路径表示，同时保持内部路径解析和测试行为不变。

## 范围
- Skill、SKILL.md、CLI 文档中的路径示例。
- 测试配置的 `~` 展开与可移植序列化。
- `~`、相对路径和绝对路径的回归测试。

## 非目标
- 不把调用图内部运行路径改为非绝对路径。
- 不修改无关项目或业务逻辑。

## 已确认要求
- 用户于 2026-08-29 确认路径可移植化方案。

## 必须遵守的约束

## 审批委托
- 状态：常规
- 范围：无
- 授权依据：无
