# workflow-agent 自修改规范

适用于修改 `SKILL.md`、`src/`、`templates/`、`tests/`、`docs/`、README、HANDOFF 和验证脚本；项目 `.workflow/` 中的台账和知识按工作流边界处理。

## 流程

1. 完成方案设计与确认。
2. 完成代码设计与确认，列出修改位置、职责、行为、验证和必要注释。
3. 实现前先写黑盒预期和失败用例。
4. 实现后根据实际分支补白盒测试。
5. 对源码修改运行 `test impact <变更文件...>`；没有静态调用边的 CLI 集成测试仍需运行 `node tests/run.ts`。
6. 最终运行 `verify-demo.bat`，检查实际输出。
7. 同步文档并追加 CHANGELOG。

用户显式委托阶段审批时，先讨论并记录本次修改中不可自动审批的事项；步骤 1、2 仍须向用户展示并取得明确确认。Agent 可自主审查设计质量，但不能代替用户确认；实现前必须由委托状态机取得 `implementation_permitted`。委托不改变步骤 3–7 的测试和交付要求。

## 模板与迁移

- `templates/vN/` 中的模板版本发布后视为不可变基线。
- 修改现有模板语义时创建下一版本目录、提升 `CURRENT_SCHEMA_VERSION`，并补充迁移测试。
- `ledger init` 只初始化新工作区或补齐当前版本缺失文件，不覆盖已有内容。
- 旧工作区使用 `ledger migrate`；自定义文件必须保留原件并产生候选，废弃文件必须可恢复。

## 测试要求

- 新行为先有仓库级 `tests/*.test.ts` 黑盒覆盖。
- 测试函数用完整验收项 ID 建立证据，例如 `test_B4_A3_...`。
- 先检查命令输出和退出码，再报告通过。
- 无法自动验证的行为写出环境、步骤和预期结果，用 `test manual` 记录实际结果。

## 注释

只注释代码本身无法表达的原因、兼容约束或反直觉行为。不要复述函数名、参数或直观控制流。

## 原则

- CLI 只做确定性操作，语义判断留给模型。
- 不通过堆叠规则限制模型；提示词按需加载，避免重复。
- 任务事实、验收状态、设计和验证证据各自只有一个职责。
- 归档使用连续版本，不使用随机 ID。
- 复杂任务可由 LLM 启用讨论状态机；讨论产出统一为目标、具体方案和验收方式，确认链完整后才执行。
