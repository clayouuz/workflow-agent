# 操作记录规则

用户主动提出、确认、纠正或要求变更的内容，默认都具有复盘价值，LLM 应立即执行 `log append --actor user` 记录；目标/方案确认、根因确定、重要实施检查点、验证结果、阻塞恢复、结论被取代或最终验收也必须记录，并使用 `--actor ai` 记录 Agent 结论。

只有普通查询、机械编辑、重复命令和完整终端输出可以跳过。同一检查点合并记录，只保存摘要、原因和 Artifact 引用；不得记录敏感信息。
